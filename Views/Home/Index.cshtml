@{
    ViewData["Title"] = "Home Page";
}

@model BooksViewModel

<h1>The Online Bookstore</h1>

@foreach (Book b in Model.Books)
{
    <div class="card card-outline-primary m-1 p-1">
        <div class="bg-light p-1">
            <h4 style="color:darkblue; font-family:Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif; letter-spacing:2px;font-size:38px;">@b.Title</h4>

            <span lang="badge bg-info" style="float:right;">
                <span>ISBN: @b.Isbn</span>
                <br />
                <span style="color:lightcoral">Price: $@b.Price</span>
            </span>
        </div>
        <ul>
            <li>Author: @b.Author</li>
            <li>Publisher: @b.Publisher</li>
            <li>Classification: @b.Classification</li>
            <li>Page Count: @b.PageCount</li>
        </ul>
        <br />

    </div>
}

<div page-blah="@Model.PageInfo" page-action="Index" page-class="btn"
     page-class-normal="btn-outline-dark" page-class-selected="btn-primary" page-classes-enabled="true"
     class="btn-group pull-right m-1"></div>